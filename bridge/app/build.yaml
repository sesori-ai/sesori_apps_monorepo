targets:
  $default:
    builders:
      json_serializable:
        options:
          any_map: true
          explicit_to_json: true
      freezed:
        options:
          format: false
          map: false
          when: false
      drift_dev:
        options:
          databases:
            default: lib/src/api/database/database.dart
            chat_history: lib/src/api/database/history/chat_history_database.dart
          write_to_columns_mixins: true
