targets:
  $default:
    builders:
      json_serializable:
        options:
          any_map: true
          explicit_to_json: true
      freezed:
        options:
          format: false
          map: false
          when: false
      drift_dev:
        options:
          write_to_columns_mixins: true
