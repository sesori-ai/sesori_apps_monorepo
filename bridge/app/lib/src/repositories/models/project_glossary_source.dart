final class ProjectGlossarySource({
  required final String projectName,
  required final String? repositoryName,
  required final List<String> trackedPaths,
  required final List<String> metadataDocuments,
});
