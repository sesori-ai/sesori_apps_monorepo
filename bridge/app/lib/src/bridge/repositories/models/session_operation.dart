enum SessionOperation {
  createSession,
  renameSession,
  getCommands,
  sendCommand,
  sendPrompt,
  getSessionMessages,
  deleteSession,
  getSession,
  archiveSession,
  abortSession,
  getChildSessions,
  getPendingQuestions,
  replyToQuestion,
  rejectQuestion,
  getPendingPermissions,
  replyToPermission,
  cleanupSession,
  updateSessionArchiveStatus,
  getProjectActivitySummaries,
  getSessionStatuses,
  getProjectQuestions,
}
