/// Enumeration of supported OAuth providers for authentication.
enum AuthProvider {
  /// GitHub OAuth provider
  github,

  /// Google OAuth provider
  google,

  /// Email/password provider
  email;
}
