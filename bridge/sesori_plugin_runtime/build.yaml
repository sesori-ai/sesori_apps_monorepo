targets:
  $default:
    builders:
      json_serializable:
        options:
          any_map: true
          explicit_to_json: true
          include_if_null: false
      freezed:
        options:
          format: false
          map: false
          when: false
