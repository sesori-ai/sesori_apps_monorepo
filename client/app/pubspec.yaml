name: sesori_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
resolution: workspace

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.8.2+1
environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  material_ui: ^1.0.1
  cupertino_ui: ^1.0.0

  liquid_glass_widgets: ^0.30.1
  http: ^1.6.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  rxdart: ^0.28.0
  flutter_bloc: ^9.1.1
  flutter_webrtc: 1.6.0
  # Before updating go_router, check its release notes for a fix to
  # https://github.com/flutter/flutter/issues/191132. Once fixed, remove the
  # explicit MaterialPage workaround in lib/core/routing/app_router.dart.
  go_router: ^17.5.0
  get_it: ^9.2.1
  injectable: ^3.0.0
  flutter_markdown_plus: ^1.0.12
  markdown: ^7.3.1
  sesori_shared:
    path: ../../shared/sesori_shared
  sesori_auth:
    path: ../module_auth
  sesori_dart_core:
    path: ../module_core
  theme_prego:
    path: ../module_prego
  flutter_secure_storage: ^11.0.0
  app_links: ^7.2.1
  url_launcher: ^6.3.2
  share_plus: ^13.3.0
  file_selector: ^1.1.0
  gal: ^2.3.3
  pasteboard: ^0.5.0
  universal_platform: ^1.1.0
  device_info_plus: ^13.2.0
  package_info_plus: ^10.2.1
  record: ^7.1.1
  path: any
  path_provider: ^2.1.6
  re_highlight: ^0.0.3
  wakelock_plus: ^1.7.0
  firebase_core: ^4.13.0
  firebase_analytics: ^12.4.6
  firebase_crashlytics: ^5.2.7
  firebase_messaging: ^16.5.0
  singular_flutter_sdk: ^1.9.0
  flutter_local_notifications: ^22.3.0
  flutter_svg: ^2.3.0
  sign_in_with_apple: ^8.1.0
  crypto: ^3.0.7
  vector_graphics: ^1.2.3
  image_picker: ^1.2.3
  flutter_keyboard_visibility:
    git:
      url: https://github.com/vespr-wallet/flutter_keyboard_visibility.git
      ref: a640afe883868faf2709c41daa391e2cd6f1fc7b
      path: flutter_keyboard_visibility

dev_dependencies:
  cryptography: ^2.9.0
  flutter_test:
    sdk: flutter
  no_slop_linter:
    path: "../../shared/no_slop_linter"
  freezed: 4.0.0-dev.3
  json_serializable: ^6.14.1
  injectable_generator: ^3.1.3
  bloc_test: ^10.0.0
  build_runner: ^2.16.0
  mocktail: ^1.0.5
  flutter_native_splash: ^2.4.8

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3
  vector_graphics_compiler: ^1.3.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Enable Swift Package Manager for iOS builds
  config:
    enable-swift-package-manager: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/archived_sessions_empty/
    - assets/images/archived_sessions_empty/2.0x/
    - assets/images/archived_sessions_empty/3.0x/
    - assets/images/bkg_png/
    - assets/images/bkg_webp/
    - assets/images/projects_onboarding/
    - assets/images/projects_onboarding/connection_graphic/
    - assets/images/projects_onboarding/connection_graphic/2.0x/
    - assets/images/projects_onboarding/connection_graphic/3.0x/
    - assets/images/projects_onboarding/empty_projects_graphic/
    - assets/images/projects_onboarding/empty_projects_graphic/2.0x/
    - assets/images/projects_onboarding/empty_projects_graphic/3.0x/
    - assets/images/projects_onboarding/info_graphic/
    - assets/images/projects_onboarding/info_graphic/2.0x/
    - assets/images/projects_onboarding/info_graphic/3.0x/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_native_splash:
  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Steps to make this work:
  # 1. Customize the parameters below.
  # 2. run the following command in the terminal:
  # dart run flutter_native_splash:create
  # or if you place this not in pubspec.yaml and not in flutter_native_splash.yaml:
  # dart run flutter_native_splash:create -p ../your-filepath.yaml
  # 3. voila, done!

  # NOTES:
  # - in case you got some trouble, cleaning up flutter project might help:
  # flutter clean ; flutter pub get
  # - To restore Flutter's default white splash screen, run the following command in the terminal:
  # dart run flutter_native_splash:remove
  # or if you place this not in pubspec.yaml and not in flutter_native_splash.yaml:
  # dart run flutter_native_splash:remove -p ../your-filepath.yaml

  # IMPORTANT NOTE: These parameter do not affect the configuration of Android 12 and later, which
  # handle splash screens differently that prior versions of Android.  Android 12 and later must be
  # configured specifically in the android_12 section below, at the very end.

  #======================================================================
  # Shared (all platforms)
  fullscreen: true # default false
  #======================================================================
  # iOS
  background_image_ios: "assets/images/bkg_png/light_mode_portrait_splash.png"
  background_image_dark_ios: "assets/images/bkg_png/dark_mode_portrait_splash.png"
  ios_content_mode: center # center, scaleToFill, scaleAspectFit, scaleAspectFill
  #======================================================================
  # Android
  # On android 12+ bkg image is not supported, so we'll just use color (for all android versions)
  android: false
  color_android: "#ffffff"
  color_dark_android: "#000000"
  android_screen_orientation: sensorPortrait
  # above stuff won't affect Android 12+ at all
  android_12:
    # background color
    color: "#ffffff"
    color_dark: "#000000"
    # center-logo background color
    icon_background_color: "#FFFFFF"
    # icon_background_color_dark: "#eeeeee"
    # center-logo
    # If this parameter is not specified, the app's launcher icon will be used instead.
    # Please note that the splash screen will be clipped to a circle on the center of the screen.
    # with background: 960×960 px (fit within circle 640px in diameter)
    # without background: 1152×1152 px (fit within circle 768px in diameter)
    # ensure that the most important design elements of your image are placed within a circular area
    # image: assets/images/logo/blank.png
    # image_dark: assets/images/logo/logo-splash2.png

  #======================================================================
  # Web
  background_image_web: "assets/images/bkg_png/light_mode_portrait_splash.png"
  background_image_dark_web: "assets/images/bkg_png/dark_mode_portrait_splash.png"
  web_image_mode: cover # center, contain, stretch, cover


  # general image for all platform (except android 12+):
  # allows you to specify an image used in the splash screen. It must be a
  # png file and should be sized for 4x pixel density.
  # image: assets/images/bkg_png/splash.png
  #image_dark:          assets/splash-invert.png
  # platform-specific image. will override general image if active:
  #image_android:       assets/splash-android.png
  #image_dark_android:  assets/splash-invert-android.png
  #image_ios:           assets/splash-ios.png
  #image_dark_ios:      assets/splash-invert-ios.png
  #image_web:           assets/splash-web.gif
  #image_dark_web:      assets/splash-invert-web.gif

  #========================================================================

  # handling concept. visit https://developer.android.com/guide/topics/ui/splash-screen

  #! ANDROID 12+ configuration:
