name: sesori_persistence
description: Shared typed persistence and encryption infrastructure for Sesori clients.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.4

dependencies:
  cryptography: ^2.9.0
  get_it: ^9.3.0
  injectable: ^3.0.0
  meta: ^1.19.0

dev_dependencies:
  build_runner: ^2.16.1
  injectable_generator: ^3.1.3
  no_slop_linter:
    path: ../../shared/no_slop_linter
  test: ^1.31.1
