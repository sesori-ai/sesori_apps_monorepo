# Step 9.a.1 — Safe diagnostic foundation

Execution evidence, not a new feature plan. The matching
`desktop-ux/logging-foundation` PR body owns inclusive publication totals and its
final immutable range, including this evidence and all other documentation.

PR #1514 merged at accepted head `8f0f12f148b42edbef096244b7b69856fe035df0`;
squash `84c034f9eba8ba490109654d8b12384d646f1cd3` has tree
`00bf6ac05fd9cf76936575279a93e70f70ca253b`. Terminal monitor CI passed 13/13.
The retained 138-case count uses B's four auth reruns in place of A's four superseded auth cases.

## Delivered boundary

- Pure core log records, sink/stdout dispatch and bounded completion; stdout remains installed.
- Required named parsing body/cause parameters; caught objects survive in auth, relay and plugin parsing.
  Presentation omits response excerpts, retaining parser offset, schema metadata or native conversion context.
- Both shell link helpers use shared origin metadata and selective known-URI replacement,
  preserving typed causes, stacks and unrelated useful diagnostics.
- Existing desktop Quit flushes after cleanup, bounded to two seconds; a failed helper stop
  still refuses exit before flushing. Failure reporting bypasses the failing sink.
- No file writers, DI registrations or `main.dart` activation in this slice.
  Preserved #1509 supplies those in 9.a.2 after a forward merge, not a history rewrite.

## Immutable checkpoints

| Point | Commit | Tree |
|---|---|---|
| Base | `cf57ce49995bfd268ad7cb5604ce84e9c35b116c` | `84ac9313777e5e50740cc0ce248f3092369efa97` |
| A | `1b8f9b5db0e64e55f08dd86deb6a0d08f39626d4` | `9b3fa594bc9f9f9f93027248c08305a2fea8185a` |
| B | `6f99208d6baae07ef0b9917360958c9832ffab47` | `748a7af560f3395070be147c0a9f00b5c0e642a2` |
| C (review fixes) | `0acc93d28a3cb779497cbc742b66564d17071f67` | `acae34455d2928c4e87e024af3acb5ce29aac545` |

A contains implementation, generated outputs and tests. B makes one non-null test
cast explicit and narrows a URI doc comment; production behavior/generated bytes
are unchanged. Integration merge `854c54bb3ab82f2bf32f8d2a063e6f96b83d980d`
brings main `5df63d1eda170a27c580ff45ba7eb89f9752bb27`, including standalone sidebar fix #1513.
The code/test/generated patch against that new base is byte-identical to Base..B:
SHA256 `80faf43d801d0ee046e24d0796a9a6056145eec8e07789b2863b2aeefab3469c`.
Publication `2ceeccb60b3ac4da42c46cc1ee28633795d9bcaf` added documentation only;
no reruns are claimed for that merge. Review fixes below extend this original boundary.

From repository root, this literal command reproduces the reviewed code/test/generated slice:

```sh
git diff --numstat cf57ce49995bfd268ad7cb5604ce84e9c35b116c 6f99208d6baae07ef0b9917360958c9832ffab47
```

26 paths; 797 changed lines = 625 additions + 172 deletions:
292 production + 459 tests + 46 generated. This is **not** the inclusive publication size.

## Generation

Pinned SDK: `/Users/alexandrudochioiu/.asdf/installs/flutter/3.47.4-stable/bin`.
Before A, cwd `client/module_auth`, ran `dart run build_runner build` without a filter:
exit 0; the runner reported `wrote 6 outputs` (builder outputs, not a count of tracked files).
Its log includes Freezed, JSON intermediate/combiner and injectable builder results.
The five tracked generated files are inventoried below. Captured index tree changed from
`ff8ea32d031b7c39840bd677741a7fd49c4cccc4` to A's tree.
Only `api_error.freezed.dart` and `api_error.g.dart` differ from base among auth outputs.
`api_response.freezed.dart`, `di/injection.config.dart` and `models/auth_state.freezed.dart`
remain byte-identical to base; no auth outputs were deleted. No generator rerun at B:
only a test and an unannotated comment changed.

## Initial focused verification

All paths below are relative to the stated cwd. `D` means pinned `dart test --reporter json`;
`F` means pinned `flutter test --no-pub --reporter json`. All test commands exited 0.

| Revision | Cwd | Runner and arguments | Cases |
|---|---|---|---:|
| A | `client/module_auth` | D `test/client/api_error_test.dart test/client/http_api_client_put_test.dart test/client/http_api_client_get_text_test.dart test/client/authenticated_http_api_client_test.dart` | 4 + 4 + 3 + 14 |
| A | `client/module_core` | D `test/logging/logging_test.dart test/platform/url_launcher_test.dart test/api/client/relay_http_client_test.dart test/repositories/plugin_repository_test.dart test/errors/remote_failure_reason_test.dart test/capabilities/voice/voice_api_test.dart test/repositories/voice_repository_test.dart` | 12 + 3 + 19 + 20 + 8 + 8 + 3 |
| A | `client/module_desktop_core` | D `test/cubits/bridge_control/bridge_control_cubit_test.dart` | 34 |
| A | `client/app` | F `test/core/external_link_test.dart` | 2 |
| A | `client/desktop` | F `test/core/external_link_test.dart` | 2 |
| B | `client/module_auth` | D `test/client/api_error_test.dart` | 4, superseding A's four |

**Initial publication: 136 distinct cases across 14 suites:** 132 at A plus four at B, not 140.
The unchanged A suites were not rerun or relabelled as B results. Structured executed
case events exclude hidden load/setup events; no failures or skipped cases.
`dart analyze --fatal-infos` passed at A in core, desktop core, app and desktop.
Auth reported one test-only nullable-cast info at A; the corrected B auth analysis passed.
Five clean owning-package results are retained, not five fresh B runs.

Coverage includes gating before stringification, context/chunking, throwing-sink fallback,
flush success/error/timeout, final cleanup-record admission before exit, failed-stop refusal,
original parsing causes/body policy, syntax/schema/native conversion diagnostics, and
URI false/throw paths with empty-link protection and original launch arguments preserved.
No additional analytics: diagnostic plumbing creates no new product action/outcome.

## Review fixes and current evidence

C applies URI-safe presentation inside the real desktop adapter's catch, before its
boolean fallback, and chunks error-bearing console messages like plain messages.
Both new assertions failed against publication production code before these fixes.
The adapter test uses the real adapter with a mocked platform channel, not an OS opener.
At C, with the same pinned SDK:

| Cwd | Command | Result |
|---|---|---|
| `client/module_core` | D `test/logging/logging_test.dart` | 13 passed |
| `client/desktop` | F `test/core/external_link_test.dart` | 3 passed |
| Both owning directories | `dart analyze --fatal-infos` | clean |

**Current retained total: 138 distinct cases:** 118 at A, four at B and 16 at C.
C supersedes A's 12 logger and two desktop-link cases; unchanged suites were not rerun.
Current analyzer evidence is core/desktop at C, auth at B, desktop core/mobile at A.
No generation was required: no model, constructor or registration changed.
The original architecture verdict remains Base..B; C changes localized method logic/tests,
not ownership or contracts. No additional architecture verdict is claimed.

## Architecture and limits

Plan review `e973a005-01ab-43fe-aaf8-eb43ebc854f6` approved the two-slice ownership.
Implementation review `089b1be0-00c3-495d-b881-7dc887886022` approved the exact Base..B
range and all 26 paths, including generated code; no findings. It excluded later docs,
file sinks and sidebar work. Complete report: `logging-foundation-implementation-architecture.md`,
SHA256 `c4c1c5b07b7c7fb123572d5dd335ced413e953608c5002501b197e9164b4ccb0`.
This is architecture approval, not native/runtime qualification.

No GUI/helper/bridge launch, stop, restart or takeover; no production DI/auth/preferences,
protected-data probe, native registration, actual OS opener or desktop smoke test ran.
No UI geometry changed, so no render fixture was needed. Actual native orderly termination,
folder opening and iOS collection remain required and unqualified. File completion/recovery
warnings and correct directory copy remain 9.a.2 work; no pending-memory bound is claimed.
Local command/generation manifests and logs: `/tmp/rose-elephant-logging-foundation-*`.
